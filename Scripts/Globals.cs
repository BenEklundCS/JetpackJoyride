using System;

namespace JetPackJoyride.Scripts {
    public static class Globals {
        public const int Gravity = 1300;
        public const float LevelMoveSpeed = 1000f;
        public static readonly Random GlobalRandom = new();
    }
}